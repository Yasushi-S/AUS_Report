# 修正実行書（AUS_Report）

---
## [2026-06-17] 実行 #1 ── creation_order.md に基づく新規開発

### 対応した指示
- creation_order.md（AUS_Report 作成発注書）

### 実施内容詳細
- `app.py` を新規作成（Flask + SQLite、CRUD 6ルート、レポート生成、PPT クリーン版置換ロジック）
- `config.py` を作成（ポート 50004、DB パス、テンプレートパス等）
- `requirements.txt`、`.gitignore`、`.env.example`、`start_app.bat` を作成
- `templates/index.html`、`edit.html`、`error.html` を作成（現行アプリのダークテーマ UI を踏襲）
- `static/css/bootstrap.min.css`、`static/js/bootstrap.bundle.min.js` をローカル配置（AUS_app からコピー）
- `.cursor/rules` を作成
- `docs/修正指示書.md` を初期化
- 既存の `aus_data.db`（568件）・PPTX テンプレートを利用して動作確認済み

### 確認事項・備考
- レポート生成テスト（2022-07月、20件）: Excel・報告書 PPT・実施数報告 PPT の3ファイル生成を確認
- 本番運用前に `.env` で `SECRET_KEY` を設定すること
- Windows ファイアウォールで TCP 50004 を許可すること
---

## [2026-06-17] 実行 #2 ── 起動時コンソール出力の文字化け修正

### 対応した指示
- docs/修正指示書.md の #1

### 実施内容詳細
- `app.py` に `import sys` を追加
- `load_dotenv()` の直後で `sys.stdout` / `sys.stderr` を `reconfigure(encoding='utf-8', errors='replace')` でUTF-8に固定
- コンソールが存在しない実行環境で `reconfigure` がエラーになる場合に備え `try/except (AttributeError, ValueError)` で握る
- 既存の print 文・起動ロジックは変更なし

### 確認事項・備考
- アプリを再起動し、起動時の案内メッセージ（日本語）が文字化けせず表示されることを確認
- アプリの機能・APIへの影響なし
---

## [2026-06-17] 実行 #3 ── タスクスケジューラへの本運用登録

### 対応した指示
- ユーザー指示（PC起動時に自動起動、シャットダウン時はWindows標準の終了処理に任せる）

### 実施内容詳細
- タスク名 `AUS_Report` を登録（トリガー: コンピューターの起動時、操作: `start_app.bat` 実行、作業ディレクトリ: `C:\Users\shita\github\AUS_Report`）
- 実行ユーザー: shita、ログオン種別: S4U（パスワード非保存でログオン不要実行）、実行レベル: 最上位の特権
- シャットダウン時の明示的な終了処理は登録せず、Windowsの標準シャットダウン処理に任せる方針とした
- CaseLogPro の既存タスク設定（vbsラッパーなしで `start_app.bat` を直接実行）に方式を統一

### 確認事項・備考
- `Get-ScheduledTask -TaskName "AUS_Report"` で State: Ready を確認
- 次回PC起動時に自動起動されるかは実機の再起動時に最終確認が必要
---
